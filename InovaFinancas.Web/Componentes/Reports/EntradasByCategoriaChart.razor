@inherits EntradasByCategoriaChartPage

@if (Data.Count > 0)
{
	<MudChart ChartType="ChartType.Pie" InputData="@Data.ToArray()" InputLabels="@Labels.ToArray()"
			Width="100%" Height="350px">

	</MudChart>
}
else
{
	<div class="d-flex align-center justify-content" style="height: 350px" >
		<MudProgressCircular Indeterminate="true" Color="Color.Primary"></MudProgressCircular>
	</div>
}
